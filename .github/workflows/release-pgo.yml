# PR-17d (v10.7.19): PGO (Profile-Guided Optimization) build для release tags.
#
# Только для `v*.*.*` tags (release builds). PR-овые CI runs используют
# обычный release profile без PGO (для скорости CI).
#
# Улучшение: ~5% throughput на hot-path bench (1.74 µs → 1.65 µs/msg).
# Полная процедура — см. docs/PERFORMANCE.md §6.

name: Release PGO build

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag для PGO-сборки (например, v10.7.20)'
        required: true
        default: ''

permissions:
  contents: read

jobs:
  pgo-build:
    name: PGO build (Linux amd64)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    concurrency:
      group: pgo-${{ github.ref }}
      cancel-in-progress: false

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust nightly toolchain (matching llvm-tools-preview)
        uses: dtolnay/rust-toolchain@nightly
        with:
          # v10.7.19 step 4: rustc 1.97 (stable) использует LLVM raw profile
          # format v10 (LLVM 19+). apt repo на ubuntu-24.04+ имеет только
          # llvm-{16,17,18} (formats v8/v9/v9) — none совместимы.
          # Решение: переключиться на nightly toolchain с компонентом
          # `llvm-tools-preview` который содержит matching LLVM profdata
          # для nightly rustc. `--profile=minimal` экономит место.
          #
          # Production build `cargo build --release` работает на nightly
          # без regression (проект совместим — cargo-rustc 1.79+ API stable).
          # Bench hot_path использует stable API (no nightly-only features).
          components: rustfmt, clippy, llvm-tools-preview
          profile: minimal

      - name: Install Rust nightly toolchain (matching llvm-tools-preview)
        uses: dtolnay/rust-toolchain@nightly
        with:
          # v10.7.19 step 4: rustc 1.97 (stable) использует LLVM raw profile
          # format v10 (LLVM 19+). apt repo на ubuntu-24.04+ имеет только
          # llvm-{16,17,18} (formats v8/v9/v9) — none совместимы.
          # Решение: переключиться на nightly toolchain с компонентом
          # `llvm-tools-preview` который содержит matching LLVM profdata
          # для nightly rustc. `--profile=minimal` экономит место.
          #
          # Production build `cargo build --release` работает на nightly
          # без regression (проект совместим — cargo-rustc 1.79+ API stable).
          # Bench hot_path использует stable API (no nightly-only features).
          components: rustfmt, clippy, llvm-tools-preview
          profile: minimal

      - name: Download matching llvm-profdata tarball
        env:
          PGO_DIR: ${{ github.workspace }}/pgo
        run: |
          # v10.7.19 step 5 (PR-Q series #75):
          # Все предыдущие PR-Q фиксы (PRs #70-#74) предполагали что
          # `rustup which llvm-profdata` или apt-installed `llvm-17` работают.
          # Это WRONG: rustup component `llvm-tools-preview` НЕ содержит
          # `llvm-profdata` binary (только llvm-objdump, llvm-nm, etc).
          # apt repo на ubuntu-24.04 имеет только llvm-{16,17,18} (raw formats
          # v8/v9/v9), несовместимые с rustc 1.97 (format v10).
          #
          # Final solution: download LLVM 20 tarball с GitHub release
          # (~200 MB). LLVM 20 (raw format v11) совместим с rustc nightly
          # 1.99 (raw format v10/11). Гарантирует matching versions.
          #
          # Если download fails — fallback к nightly toolchain llvm-profdata
          # через apt (emergency). Смотри Tier 1 в step 3.
          set -e
          LLVM_VER=20.1.0
          LLVM_TARBALL="clang+llvm-${LLVM_VER}-x86_64-unknown-linux-gnu.tar.xz"
          LLVM_URL="https://github.com/llvm/llvm-project/releases/download/llvmorg-${LLVM_VER}/${LLVM_TARBALL}"
          echo "Downloading $LLVM_URL (~200 MB)..."
          mkdir -p /tmp/llvm-bin
          curl -fSL --connect-timeout 30 --max-time 600 "$LLVM_URL" \
            -o "/tmp/$LLVM_TARBALL"
          tar -xJf "/tmp/$LLVM_TARBALL" -C /tmp/llvm-bin --strip-components=1
          ls -la /tmp/llvm-bin/bin/llvm-profdata
          /tmp/llvm-bin/bin/llvm-profdata --version
          # Сохраняем путь для downstream steps.
          echo "LLVM_BIN=/tmp/llvm-bin/bin" >> "$GITHUB_ENV"
          echo "=== llvm-profdata location after download ==="
          ls -la /tmp/llvm-bin/bin/llvm-profdata
          which llvm-profdata || true
          ls -la /usr/bin/llvm-profdata 2>&1 || true

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: ${{ runner.os }}-cargo-pgo
          cache-on-failure: true

      - name: PGO step 1/3 — build with profile-generate
        env:
          # Используем nightly (установлен выше) для consistent LLVM version.
          RUSTUP_TOOLCHAIN: nightly
          PGO_DIR: ${{ github.workspace }}/pgo
        run: |
          mkdir -p "$PGO_DIR"
          # -Cprofile-generate= записывает .profraw файлы в $PGO_DIR при запуске бинаря.
          RUSTFLAGS="-Cprofile-generate=$PGO_DIR" \
            cargo build --release --bench hot_path

      - name: PGO step 2/3 — run representative workload (bench)
        env:
          RUSTUP_TOOLCHAIN: nightly
          PGO_DIR: ${{ github.workspace }}/pgo
        run: |
          # Прогон benches для сбора representative profile данных.
          # Criterion binary сам по себе нагружает hot-path достаточно для профиля.
          RUSTFLAGS="-Cprofile-generate=$PGO_DIR" \
            ./target/release/deps/hot_path-* --bench --quick

      - name: PGO step 3/3 — merge profdata + final build
        env:
          RUSTUP_TOOLCHAIN: nightly
          PGO_DIR: ${{ github.workspace }}/pgo
        run: |
          # Resolve llvm-profdata:
          # 1. `/tmp/llvm-bin/bin/llvm-profdata` (downloaded in previous step,
          #    matching nightly rustc LLVM 20+) — **PRIMARY**
          # 2. `/usr/lib/llvm-{17,16,15}/bin/llvm-profdata` (apt-installed,
          #    НЕ совместим с rustc 1.97 формат v10 — fallback only для MSRV)
          # 3. `which llvm-profdata` (apt default symlink, possible format mismatch)
          # 4. generic /usr/lib/llvm-*/bin/llvm-profdata
          LLVM_PROFDATA=""

          # Tier 1: downloaded LLVM 20 tarball (matching nightly).
          if [ -x "${LLVM_BIN:-/nonexistent}/llvm-profdata" ]; then
            LLVM_PROFDATA="${LLVM_BIN}/llvm-profdata"
          fi

          # Tier 2: apt-installed llvm-{17,16,15}.
          if [ -z "$LLVM_PROFDATA" ] || [ ! -x "$LLVM_PROFDATA" ]; then
            for v in 17 16 15; do
              if [ -x "/usr/lib/llvm-${v}/bin/llvm-profdata" ]; then
                LLVM_PROFDATA="/usr/lib/llvm-${v}/bin/llvm-profdata"
                break
              fi
            done
          fi

          # Tier 3: /usr/bin/llvm-profdata (apt default symlink).
          if [ -z "$LLVM_PROFDATA" ] || [ ! -x "$LLVM_PROFDATA" ]; then
            LLVM_PROFDATA="$(which llvm-profdata 2>/dev/null || true)"
          fi

          # Tier 4: generic versioned path.
          if [ -z "$LLVM_PROFDATA" ] || [ ! -x "$LLVM_PROFDATA" ]; then
            LLVM_PROFDATA="$(ls -d /usr/lib/llvm-*/bin/llvm-profdata 2>/dev/null | head -1 || true)"
          fi

          if [ ! -x "$LLVM_PROFDATA" ]; then
            echo "::error::llvm-profdata binary not found. Tried: /tmp/llvm-bin (downloaded) / llvm-{17,16,15} / which / /usr/lib/llvm-*/bin."
            exit 1
          fi
          echo "Using llvm-profdata: $LLVM_PROFDATA"
          "$LLVM_PROFDATA" --version

          # Merge .profraw → merged.profdata
          echo "::group::Merging $(ls "$PGO_DIR"/*.profraw 2>/dev/null | wc -l | tr -d ' ') profraw files"
          ls "$PGO_DIR"/*.profraw 2>/dev/null | head -5
          "$LLVM_PROFDATA" merge -o "$PGO_DIR/merged.profdata" "$PGO_DIR"/*.profraw
          ls -la "$PGO_DIR/merged.profdata"
          echo "::endgroup::"

          # Final build with profile-use
          echo "::group::Building optimized binary with merged.profdata"
          RUSTFLAGS="-Cprofile-use=$PGO_DIR/merged.profdata" \
            cargo build --release
          echo "::endgroup::"

      - name: Verify PGO improvement (sanity bench)
        run: |
          echo "::group::PGO verification — bench should show ~1.65 µs/msg (vs ~1.74 без PGO)"
          ./target/release/deps/hot_path-* --bench --quick 2>&1 \
            | grep -E "rfc5424_with_faker|thrpt" \
            | head -5
          echo "::endgroup::"

      - name: Upload PGO-optimized binary
        uses: actions/upload-artifact@v4
        with:
          name: syslog-generator-pgo-${{ github.ref_name }}
          path: |
            target/release/syslog-generator
            pgo/merged.profdata
          if-no-files-found: error
          retention-days: 90

      - name: Summary
        if: always()
        run: |
          echo "## PGO build summary" >> $GITHUB_STEP_SUMMARY
          echo "- Tag: ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- Profile files: $(ls ${{ github.workspace }}/pgo/*.profraw 2>/dev/null | wc -l)" >> $GITHUB_STEP_SUMMARY
          echo "- Merged profdata: $(ls -la ${{ github.workspace }}/pgo/merged.profdata 2>/dev/null | awk '{print $5}') bytes" >> $GITHUB_STEP_SUMMARY
          echo "- See docs/PERFORMANCE.md §6 for details" >> $GITHUB_STEP_SUMMARY